spring:
  application:
    name: capture-simulator
  profiles:
    active: dev
  cloud:
    nacos:
      # 服务注册
      discovery:
        server-addr: ${REGISTER_HOST:175.168.13.65}:${REGISTER_PORT:30093}
        username: ${REGISTER_USERNAME:nacos}
        password: ${REGISTER_PASSWORD:1qaz2wsx}
        group: ${REGISTER_GROUP:DEFAULT_GROUP}
      # 服务动态配置
      config:
        server-addr: ${REGISTER_HOST:175.168.13.65}:${REGISTER_PORT:30093}
        username: ${REGISTER_USERNAME:nacos}
        password: ${REGISTER_PASSWORD:1qaz2wsx}
        file-extension: yml
        refresh-enabled: true
        enabled: true
        group: ${REGISTER_GROUP:DEFAULT_GROUP}